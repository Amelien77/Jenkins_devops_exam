apiVersion: v1
kind: Pod
metadata:
  name: test-connection
spec:
  containers:
    - name: test-connection
      image: busybox
      command: ["sh", "-c", "nc -zv {{ .Values.services.cast-service.service.type }} {{ .Values.services.cast-service.service.port }} || exit 1"]
