apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cast-service-claim
  namespace: qa
  labels:
    app: castservice
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
