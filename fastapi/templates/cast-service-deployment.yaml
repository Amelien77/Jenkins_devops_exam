apiVersion: apps/v1
kind: Deployment
metadata:
  name: cast-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: cast-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: cast-service
    spec:
      containers:
        - name: cast-service
          image: "{{ .Values.services.cast-service.image.repository }}:{{ .Values.services.cast-service.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.cast-service.service.port }}
          env:
            {{- range .Values.services.cast-service.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
            {{- end }}
          volumeMounts:
            {{- range .Values.services.cast-service.volumeMounts }}
            - name: "{{ .name }}"
              mountPath: "{{ .mountPath }}"
            {{- end }}
      volumes:
        {{- range .Values.services.cast-service.volumes }}
        - name: "{{ .name }}"
          persistentVolumeClaim:
            claimName: "{{ .persistentVolumeClaim.claimName }}"
        {{- end }}
