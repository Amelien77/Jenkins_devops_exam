apiVersion: apps/v1
kind: Deployment
metadata:
  name: castservice
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: castservice
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: castservice
    spec:
      containers:
        - name: castservice
          image: "{{ .Values.services.castservice.image.repository }}:{{ .Values.services.castservice.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.castservice.service.port }}
          env:
            {{- range .Values.services.castservice.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
            {{- end }}
          volumeMounts:
            {{- range .Values.services.castservice.volumeMounts }}
            - name: "{{ .name }}"
              mountPath: "{{ .mountPath }}"
            {{- end }}
      volumes:
        {{- range .Values.services.castservice.volumes }}
        - name: "{{ .name }}"
          persistentVolumeClaim:
            claimName: "{{ .persistentVolumeClaim.claimName }}"
        {{- end }}
