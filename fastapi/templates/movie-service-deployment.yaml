apiVersion: apps/v1
kind: Deployment
metadata:
  name: movie-service
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: movie-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: movie-service
    spec:
      containers:
        - name: movie-service
          image: "{{ .Values.services.movieservice.image.repository }}:{{ .Values.services.movieservice.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.services.movie-service.service.port }}
          env:
            {{- range .Values.services.movie-service.env }}
            - name: "{{ .name }}"
              value: "{{ .value }}"
            {{- end }}
          volumeMounts:
            {{- range .Values.services.movie-service.volumeMounts }}
            - name: "{{ .name }}"
              mountPath: "{{ .mountPath }}"
            {{- end }}
      volumes:
        {{- range .Values.services.movie-service.volumes }}
        - name: "{{ .name }}"
          persistentVolumeClaim:
            claimName: "{{ .persistentVolumeClaim.claimName }}"
        {{- end }}
